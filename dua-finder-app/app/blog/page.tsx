import Link from 'next/link';import { Metadata } from 'next';import { posts } from '@/data/blog';
export const metadata:Metadata={title:'Islamic Blog',description:'Read Islamic reminders, Quran verses, Ramadan guides, Islamic lifestyle, parenting, and education articles.'};
export default function BlogPage(){return <div className="containerx py-12"><h1 className="text-4xl font-bold">Islamic Blog</h1><p className="mt-3 max-w-2xl text-slate-600 dark:text-slate-300">Helpful Islamic reminders and educational guides written in simple language.</p><div className="mt-10 grid gap-6 md:grid-cols-3">{posts.map(p=><article key={p.slug} className="card p-6"><span className="text-xs font-semibold text-brand dark:text-gold">{p.category}</span><h2 className="mt-3 text-2xl font-bold"><Link href={`/blog/${p.slug}`}>{p.title}</Link></h2><p className="mt-3 text-slate-600 dark:text-slate-300">{p.excerpt}</p><Link className="mt-5 inline-block font-semibold text-brand dark:text-gold" href={`/blog/${p.slug}`}>Read article →</Link></article>)}</div></div>}
